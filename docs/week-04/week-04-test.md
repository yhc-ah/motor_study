# 第四周验证记录

用户确认week1、week2、week3实验已完成；本记录仅说明本次新增week4的验证。基础为week3 a168430。所有硬件结果当前为NOT RUN，没有创建week-04-pass。

## 软件证据

- 按测试先行实现PWM/编码器/滤波计算：初始桩执行产生3221个失败，实现后27255项数学检查通过，包含1001档占空比×频率/对齐组合和回绕/滤波边界。
- ADC交接与应用命令测试先对空实现产生预期断言失败，再实现；冻结半区边界的双块保留新增回归先失败后通过。
- 实际 `bsp_week4.c` 使用仓库真实HAL类型/寄存器定义进行主机模拟：初始化失败不访问空DMA句柄、正常完整块、7点尾块、停止边界双块、4步编码器最终位移、PWM停止低电平、停止失败锁定等通过。HAL stop桩要求全局IRQ仍启用，避免超时失效。
- PC工具对丢失ACK、碎片/CRC、开始前/停止前交错数据、尾块闭合、空采集、短覆盖、错误成功标记、故障注入、SVG时间缺口与初始化窗口均有回归测试。
- 合成阶跃仅验证滤波数学：5ms组t63约5ms、10–90%约11ms；20ms组约20ms、44ms。不是实物波形。
- 独立固件规格/质量审查发现并修复初始化失败、全局关中断中HAL超时、最终编码器计数及停止半区数据保留问题；PC审查发现并修复空采集、覆盖不足、状态误停、图表缺口、有限编码器等待和错误成功标记问题。

最终命令、构建尺寸与总数在交付核验后补记；可复现入口 `./tests/host/run_week4_tests.ps1`，Keil工程 `MDK-ARM/stm32-realtime-bsp.uvprojx`。

## 实物验收（均未执行）

| 实验 | 目标 | 状态/需要保存的证据 |
|---|---|---|
|0|接线、时钟、重建与下载回归|NOT RUN：真实接线照片、DAP下载记录、串口回归|
|1|1/10/20kHz，10/50/90%，中心对齐及端点|NOT RUN：逻辑分析仪波形和量化误差|
|1动态|≥100次更新无已检测异常脉冲|NOT RUN：更新前后脉宽/边沿截图|
|2|100/1000Hz，两方向各1000周期及回绕|NOT RUN：A/B波形、最终±4000及原始CNT|
|3|三点电压×84/480周期|NOT RUN：六组CSV、万用表、VREF及偏差|
|4|1kHz±1%，连续≥60s，数据闭合|NOT RUN：原始数据、触发参考、HT/TC、末尾点数|
|4故障|250ms消费暂停被检测且可恢复|NOT RUN：覆盖/缺口计数和独立故障记录|
|5|同源噪声统计与真实响应|NOT RUN：真实CSV/图、初始化排除与链路延迟|
|6|PWM＋编码器＋ADC＋日志连续600s|NOT RUN：正常60s窗口、联合波形、错误与延迟计数|

本机仅发现Bluetooth COM3/4/5/6，没有识别到开发板串口，未进行烧录或任何实物测量。源码、主机模型、配置寄存器和编译通过不替代独立仪器证据。本周默认文本状态，未将LCD显示冒充已实现/已验收。

## 最终软件核验（2026-09-08）

- run_week4_tests.ps1 退出0：13个C测试程序全部通过，包含27255项数学检查；37项Python测试通过（原18项＋week4新增19项），UART编解码自检通过。
- Keil ARMCC5.06 update7 build960：0 Error(s), 0 Warning(s)。Code=18508、RO-data=532、RW-data=312、ZI-data=8520字节，生成AXF/HEX；最终增量构建8秒。
- 两次工程同步后SHA256一致；54个项目源文件引用均存在，无重复。git diff --check通过。未声称CubeMX GUI重新生成已验证。
- step-test命令输出synthetic=true含义的source说明，5ms组5/11ms、20ms组20/44ms，与离散公式一致；合成输出不计入实物验收。
- 仅源码/测试/工具/文档进入提交，Keil生成的会话与构建报告文件排除。
