# 基线与边界

前四周完成状态以用户确认作为本次前提。软件基线固定在week4 b548b0d；week5远端初始仅含week1，已通过普通快进方式承接week4，无强推、无改写前四周历史。

原目录 `E:\motor_prj\stm32-realtime-bsp` 仍为week2并有用户已有的RTE头、uvoptx、构建HTML修改。新增独立目录 `E:\motor_prj\stm32-realtime-bsp-week5` 进行本周修改。

本次基线主机回归全部通过；基线构建尺寸来自week4提交内已记录的Keil真实构建，不把本次增量构建说成重新完成前四周实物实验。

新增内容范围：MCU分层、联合调度/测量、F411最小设备移植、静态C++包装、故障与长时验收工具。未引入RTOS、CAN、PID、FOC或动力输出。

依赖缺口：实际LCD和温湿度驱动未包含在所提供版本中；已向用户询问具体型号/目录、F411板型接线及Flash空闲区。程序保留明确缺失能力，不能猜测器件后填入假数据。硬件连接缺失不妨碍主机测试和目标固件构建，但阻止完整实物通过结论。
