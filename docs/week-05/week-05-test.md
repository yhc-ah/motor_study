# 第五周验证记录

日期：2026-09-09。基线：week4 `b548b0d1318ed6e6b5302b81c94c6a4e1ed9864b`。
用户确认前四周实验已完成；本次没有把前几周历史文档中的待测项重新宣称为失败。

## 软件检查

最终全量回归：17个C测试可执行程序、62个Python测试通过（另含C内部27255条数学检查和固定种子10000例协议矩阵）。

- [x] 在独立week5工作目录中承接完整week4代码，保留原目录3项本地修改与weeks1–4分支。
- [x] 继承通信、MPU、Flash、PWM、ADC、编码器回归。
- [x] 绝对周期调度：32位微秒回绕、漏周期跳过、900000次到期、64位时间累计。
- [x] Flash协议模型：显式边界、5扇区空白检查、180条64B写入与读回、scratch隔离、损坏后锁定。
- [x] 整合应用模型：缺失适配器拒绝启动、原始帧、开始/完成时间、失约计数、Flash重新准备失败清除就绪标记。
- [x] 实际BSP与模拟寄存器：180000编码器周期、720000状态变化及最终位置闭合，原ADC尾块/覆盖测试。
- [x] 实际C解析器固定种子10000例：5000合法，错帧零执行，恢复成功；PC坏CRC洪泛生成与执行计数检查。
- [x] PC流式记录：1800秒等效900000IMU/1800000ADC数据的固定内存状态测试、坏ACK清理、尾块、间隙、时间回退和验收边界。
- [x] F407 Keil实际构建；F411 ARMCC5 C/C++实际编译、链接、HEX转换，引用同一设备源码。

可复现入口：`./tests/host/run_week5_tests.ps1`。完整主机输出保存在 [host-validation.log](host-validation.log)。构建尺寸及来源见 [map-size-comparison.md](map-size-comparison.md)。最终交付前再次运行全量检查，以该日志末尾的通过状态为准。

## 实物验收：尚未完成

本机枚举仅发现蓝牙COM3/4/5/6，没有可确认的板卡串口。本次未烧录、未操作电位器/接线、未生成或伪造硬件采样数据。

- [ ] 确认真实温湿度型号、1Hz非阻塞驱动及LCD局部更新驱动，并实现`W5Extras_*`适配。目前默认能力缺失，stage3/4/5拒绝。
- [ ] 确认Flash空闲20KiB地址，实物准备与180条日志核对。
- [ ] F407全模块1800秒正常运行，1Hz统计、完整原始记录、开始/中段/结束波形。
- [ ] ADC参考1kHz±1%、最大周期误差<100μs；IMU事务开始最大周期误差<200μs、零漏槽；ADC复制完成延迟<80ms。
- [ ] F411 C/CPP各按相同接线验证所选设备，至少5分钟采集与完整60秒原始记录；不能用构建代替移植实测。
- [ ] UART实物10000例与60秒洪泛、I2C断线3次2秒恢复、ADC250ms过载、LCD/Flash长任务实验。
- [ ] 口述分层、时钟、DMA所有权、启动流程，现场五分钟复现。

因此当前状态是 **软件交付与测试可复现，完整第五周实物验收未通过**。`hardware_verdict=NOT_ASSESSED` 是刻意保留的真实边界，不创建`week-05-pass`标签。

所有实物输出应使用新目录保存版本SHA、接线照片/型号、原始二进制、CSV、统计、波形、故障标记和最终判据。合成测试结果只属于主机模型，不放进实物data目录。
